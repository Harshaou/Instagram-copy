import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  profileImg: {
    height: 80,
    width: 80,
    borderRadius: 40,
  },
});

export default styles;
